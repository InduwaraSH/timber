import 'package:flutter/material.dart';

class pgtwo extends StatelessWidget {
  const pgtwo({super.key});

  @override
  Widget build(BuildContext context) {
     return Container(color: Colors.green);
  }
}