import 'package:flutter/material.dart';

class pgthree extends StatelessWidget {
  final String office_location;
  const pgthree({super.key, required this.office_location});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("datccdcccdca", style: TextStyle(color: Colors.black))],
      ),
    );
  }
}
